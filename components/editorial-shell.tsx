"use client";
import {useState,type ReactNode} from "react";
import {Menu,ArrowUpRight} from "lucide-react";
import {Brand} from "@/components/site-brand";
import {SocialLinks} from "@/components/social-links";
import {Sheet,SheetContent,SheetTitle,SheetDescription} from "@/components/ui/sheet";
const links=[["Our expertise","/#expertise"],["In practice","/in-practice/crm"],["Events","/events"],["Our firm","/our-firm"]];
export function EditorialShell({children,current}:{children:ReactNode;current:string}){const [open,setOpen]=useState(false);return <><a className="skip-link" href="#main">Skip to content</a><header className="site-header"><div className="header-inner"><Brand/><nav className="desktop-nav" aria-label="Main navigation">{links.map(([t,u])=><a href={u} key={u} aria-current={current===u?"page":undefined}>{t}</a>)}</nav><a href="/start" className="header-contact">Start a conversation <ArrowUpRight size={17}/></a><button className="menu-button" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button></div></header><main id="main">{children}</main><footer className="site-footer"><div className="container footer-top"><Brand large/><div><span className="eyebrow">Business strategy & implementation</span><p>Detroit, Michigan</p></div><a href="/start" className="back-top">Start a conversation <ArrowUpRight size={17}/></a></div><div className="container footer-bottom"><span>© {new Date().getFullYear()} Brown & Gaines</span><SocialLinks/><a href="/">Back to the firm</a></div></footer><Sheet open={open} onOpenChange={setOpen}><SheetContent className="mobile-sheet"><SheetTitle>Brown & Gaines</SheetTitle><SheetDescription>Business strategy & implementation</SheetDescription><nav aria-label="Mobile navigation">{links.map(([t,u])=><a href={u} key={u}>{t}<ArrowUpRight size={18}/></a>)}</nav><a href="/start" className="button button-blue">Start a conversation</a></SheetContent></Sheet></>}
